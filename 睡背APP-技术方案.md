# 睡背APP · 技术方案（v0.3 · 含实施状态）

> 定位：给初中女儿晚上睡觉时听背诵课本内容。
> 工作流（孩子独立操作）：打开 APP → 拍照课本页 → 自动识别文字 → 编辑确认 → 点播放 → 选「循环 N 遍」或「定时」→ 手机放一边息屏。
> 已确认约束：安卓小米手机；**不用本地电脑跑 TTS**；用**免费线上的语音服务**；音色先做一个标准的；家长不介入日常操作。
> 原则：能用开源就用开源，避免重复造轮子。

---

## 〇、实施状态速览（v0.3 新增）

| 项 | 状态 |
| --- | --- |
| 开发环境（Flutter/JDK/Android SDK） | ✅ 已装好，全部在 D 盘（详见交接文档） |
| Phase 0 技术验证（edge-tts 直连） | ✅ 完成：直连可用但会限流，需重试；Dart 包验证通过 |
| MVP 第一阶段（文字→晓晓→循环/定时播放） | ✅ 完成：debug APK 已构建并交付（149MB） |
| 拍照识别（OCR） | ⬜ 未做（包已核实：flutter_paddle_ocr_v5） |
| 后台播放/锁屏控制（audio_service） | ⬜ 未做 |
| 内容管理（科目/课文归档） | ⬜ 未做 |
| 多音色 | ⬜ 未做（MVP 固定晓晓） |
| PC 端预览（模拟器） | ⬜ 方案 A 已确认，待安装（全走 D 盘） |
| release 体积优化 | ⬜ 未做（预计可从 149MB → 30-40MB） |

> ⚠️ 新接手 AI 必读：**《睡背APP-交接文档.md》**（环境、产物、坑、下一步都在里面）。

---

## 一、需求还原（更新版）

| 角色 | 做什么 |
| --- | --- |
| 女儿（使用者） | 拍照 → 识别 → 播放，全程自己操作，界面要简单到 3~4 步 |
| 家长（一次性设置） | 装好 APP 后，设一次默认音色/语速/常用时长即可，之后不介入 |

核心体验要求：音色温柔自然、到点自动渐弱停止、循环 N 遍、不伤眼、操作少、播放稳定不卡、无网也能播（已生成的音频）。

---

## 二、核心设计决策：生成与播放解耦（两段式，仍成立）

- **生成端**：全部在手机 APP 内完成——拍照 → 本地 OCR → 文字 → 调用**免费线上 TTS** 生成音频 → 缓存到本地。需要网络，但只在一段文字首次录入时发生（几秒~十几秒）。
- **播放端（睡前）**：**纯本地播放**已缓存好的音频文件。不依赖网络、不卡顿、可熄屏、可后台保活。
- 「定时停止」「循环 N 遍」只是播放器逻辑，与 TTS 无关，简单可靠。
- 同一段文字只生成一次（按 文本+音色+语速 去重缓存）。

**关键优势（v0.2 更新）**：TTS 走「Flutter 直连微软免费接口」的方式，**不需要任何服务器、不需要本地电脑、不需要 API Key**——彻底满足你的约束。

---

## 三、功能模块与开源选型（v0.3）

### 3.1 拍照识别（OCR）——孩子独立操作的入口（v0.3 修正选型）

| 方案 | 说明 | 结论 |
| --- | --- | --- |
| **flutter_paddle_ocr_v5**（pub.dev，0.0.3） | Android 原生插件（包名 com.huyphan.flutter_paddle_ocr），PP-OCRv5 + ONNX，**本地离线**中文识别，不依赖 Google 服务（国内小米手机友好） | ✅ 首选：APP 内直接拍照识别 |
| ~~rapid_kit~~ | ~~Flutter FFI 插件…~~ | ❌ **已核实淘汰**：rapid_kit 是 IoT 平台包，不是 OCR |
| ~~flutter_onnx_ocr~~ | | ❌ 仅支持 macOS/Windows，安卓不可用 |
| RapidOcrAndroidOnnx（原生） | RapidAI 官方安卓 OCR 库，若 flutter_paddle_ocr_v5 不满足可走 MethodChannel 封装 | 备选 |
| 豆包识别 → 粘贴文字 | 家长协助时的辅助入口（复制文本直接粘贴） | 辅助 |

识别流程：拍照（或选图）→ 识别 → 文字可编辑确认（识别错字随手改）→ 保存。

### 3.2 语音合成（TTS）——免费线上，零服务器

| 方案 | 许可 | 说明 | 结论 |
| --- | --- | --- | --- |
| **edge_tts（Flutter 包）** | MIT | pub.dev 现成 Dart 包，**APP 内直连微软免费神经语音接口**，无 Key、无服务器；中文音色 20+（晓晓/云希/晓伊/云扬等） | ✅ 首选：v1 固定默认音色 |
| **Cloudflare Worker 中转**（可选） | 开源（社区 worker 项目） | 若国内直连微软接口不稳/限流，部署一个免费 Worker（免费额度约 10 万次/天）做中转，APP 改调自己的 HTTPS 地址即可 | 增强：直连不稳时再加，无需本地电脑 |
| **豆包语音 API**（火山方舟 seed-tts） | 云 API | 官方稳定、国内快；新用户免费试用（语音合成约 5000 字符），正式按量约 0.0003 元/字符 | 兜底：Edge-TTS 彻底失效时的官方替代 |

**v1 固定音色**：`zh-CN-XiaoxiaoNeural`（晓晓，温柔女声，最适合睡前朗读）。UI 不做音色选择，架构里保留音色字段，二期再加。

### 3.3 APP 框架与播放

- **Flutter**（确认）：Android 优先（小米），以后可出 iOS/鸿蒙。
- 播放：**just_audio + audio_service**——后台播放、锁屏控制、播放列表、循环模式、音量渐弱（audio_service 未集成，属下一步）。
- （备选）Android 原生 Kotlin + Media3/ExoPlayer：只做安卓且求最稳时用。

### 3.4 定时停止 / 循环 N 遍 / 睡眠模式

| 功能 | 实现思路 |
| --- | --- |
| 定时停止 | 15/30/45/60 分钟可选 → 结束前 30~60 秒音量渐弱 → 自动暂停（just_audio setVolume） |
| 循环 N 遍 | 播放完成事件计数：单条循环 N 遍 / 整个列表循环 N 遍，到 N 自动停 |
| 语速 | 生成时按语速参数合成（edge_tts rate），播放不变速，音质更好 |
| 睡眠模式 | 熄屏播放（前台服务保活）、通知栏/锁屏控制、夜间深色主题、大按钮 |

MVP 默认值（用户已确认由开发方定）：循环默认 3 遍（可选 1/2/3/5）；定时默认 30 分钟（可选 15/30/45/60）。

### 3.5 数据管理

- 本地 SQLite（Flutter 用 drift）：科目（语文/英语/道法/历史…）→ 课文/章节 → 段落条目。
- 每条：标题、原文文本、音频路径、音色、语速、创建时间、播放次数。
- 音频按 `文本+音色+语速` hash 去重缓存；原图可选归档。

---

## 四、总体架构（v0.2：零服务器）

```
┌─ 手机 APP（小米 · 安卓）──────────────────────────────────────┐
│  ① 拍照/选图 ──▶ 本地 OCR ──▶ 文字编辑确认 ──▶ 入库      │
│  ② 播放页：默认音色「晓晓」 ──▶ edge_tts 直连微软免费接口（在线）    │
│       │  （直连不稳 → Cloudflare Worker 免费中转；兜底豆包 API）     │
│       ▼                                                         │
│  音频文件缓存（mp3，按文本+音色+语速去重）                             │
│  ③ 睡前播放：just_audio 本地播放 ──▶ 循环 N 遍 / 定时渐弱 / 息屏      │
└────────────────────────────────────────────────────────────────┘
  无本地电脑 · 无自建服务器 · TTS 在线免费 · 播放全程本地
```

---

## 五、里程碑与工作量（v0.3 加状态）

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| **Phase 0 技术验证** | 建 Flutter 工程，用 edge_tts 包生成「晓晓」试听音频，验证国内直连成功率 | ✅ 完成 |
| **Phase 1 MVP** | （拆为两段）① 文字粘贴→晓晓生成→循环/定时播放（debug APK）② 拍照识别（flutter_paddle_ocr_v5）→ 文字编辑 → 保存 → 息屏播放 + 锁屏控制（audio_service） | ① ✅ ② ⬜ |
| **Phase 2 打磨** | 多音色选择、背诵模式（只听不显示文字）、每日计划、播放统计、UI 细节打磨 | ⬜ |
| **PC 端预览** | Android 模拟器（方案 A，已确认）：PC 上模拟手机界面，flutter run 热重载，不再手动装 APK | ⬜ 待安装（全走 D 盘） |
| **release 优化** | 单 ABI（arm64-v8a）+ 压缩：APK 149MB → 预计 30-40MB | ⬜ |

初期不做：账号体系、云同步、iOS。

---

## 六、风险与对策

| 风险 | 对策 |
| --- | --- |
| Edge-TTS 国内直连不稳/限流 | 已验证：直连可用但会限流 → APP 内已内置 5 次指数退避重试（实测 Dart 包一次成功）；再不稳 → Cloudflare Worker 免费中转；兜底豆包 API |
| 首次生成需联网等几秒~十几秒 | 生成中给进度提示；生成后永久缓存，睡前播放完全离线 |
| 没网时新内容无法生成 | 播放已缓存内容不受影响；家长可提前批量生成 |
| 后台播放被杀 | 前台服务 + 常驻通知（Android，audio_service 未集成，属下一步）；锁屏控制 |
| 识别错字 | 识别后强制进编辑页确认，可改 |
| 课本内容版权 | 仅个人学习用途，本地存储不外传 |

---

## 七、已确认决策记录（原"待确认"已全部拍板）

1. **默认音色**：晓晓（zh-CN-XiaoxiaoNeural）✅（用户已确认；小米系统音色作为二期可选入口，音质不如晓晓）
2. **安装方式**：APK 侧载 ✅（前期调试走 PC 模拟器/USB）
3. **循环/定时**：都做，MVP 默认循环 3 遍 / 定时 30 分钟，参数可改
4. **零本地 TTS**：否掉 CosyVoice2 自部署路线 ✅（明确不用本地电脑跑 TTS）
5. **音色数量**：先固定一个标准音色，不着急加 ✅
6. **PC 预览方案**：Android 模拟器（方案 A）✅

---

## 八、参考资料

- edge_tts（Flutter 包，MIT）：https://pub.dev/packages/edge_tts
- flutter_paddle_ocr_v5（Android 离线 OCR，PP-OCRv5+ONNX）：https://pub.dev/packages/flutter_paddle_ocr_v5
- Edge-TTS 原项目（Python）：https://github.com/rany2/edge-tts
- Edge-TTS + Cloudflare Worker 免费中转教程：https://juejin.cn/post/7559435122451611702 、https://jishuzhan.net/article/1981953548930842626
- 豆包大模型/语音（火山引擎）：https://www.volcengine.com/product/doubao
- Flutter 音频：just_audio（https://pub.dev/packages/just_audio）、audio_service（https://pub.dev/packages/audio_service）

---

## 九、交接指引

新接手 AI 请先读 **《睡背APP-交接文档.md》**（项目根目录），内含：开发环境路径、构建命令、已完成产物、代码要点、已知坑、协作规则、下一步计划。
